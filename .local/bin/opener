#!/bin/sh

case $(file --mime-type -b -L "$1") in
    text/*|application/csv|application/json|inode/x-empty) IFS=':'; $EDITOR "$1";;
    audio/*|image/*|video/*) m "$1" -quiet >/dev/null 2>&1 & ;;
    *) IFS=':'; for f in "$1"; do xdg-open "$f" > /dev/null 2> /dev/null & done;;
esac
